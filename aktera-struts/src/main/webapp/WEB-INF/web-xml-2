
	<filter>
		<filter-name>EncodingFilter</filter-name>
		<filter-class>de.iritgo.aktera.struts.EncodingFilter</filter-class>
		<init-param>
			<param-name>encodings</param-name>
			<param-value>UTF-8,ISO-8859-1</param-value>
		</init-param>
		<init-param>
			<param-name>inputEncodingParameterName</param-name>
			<param-value>ie</param-value>
		</init-param>
	</filter>

	<filter-mapping>
		<filter-name>EncodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<listener>
		<listener-class>de.iritgo.aktera.struts.KeelContextListener</listener-class>
	</listener>

	<listener>
		<listener-class>de.iritgo.aktera.struts.KeelSessionListener</listener-class>
	</listener>

	<!-- Application defined context listeners -->

